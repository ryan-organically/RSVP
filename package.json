{
  "name": "rsvp-reader",
  "version": "2.0.0",
  "private": true,
  "description": "Local-first speed-reading (RSVP) web app. No accounts, no servers, no data leaves your browser.",
  "license": "MIT",
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'static site'"
  }
}
